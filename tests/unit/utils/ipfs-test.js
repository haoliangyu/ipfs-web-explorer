import ipfs from 'ipfs-explorer/utils/ipfs';
import { module, test } from 'qunit';

module('Unit | Utility | ipfs');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ipfs();
  assert.ok(result);
});
